# Popular Vacancy Project

Популярные вакансии - секция-слайдер. Создан на стеке: Vite, SCSS и запускается в Docker через Nginx.

## 🚀 Запуск приложения

### В production-режиме (через Docker)
Выполните в корне проекта команду:

```bash
docker-compose up --build -d
```
Проект будет доступен по адресу: http://localhost

### Другие команды (Docker):
```bash
docker-compose up -d
docker-compose down
```

### Проект берется из папки /dist
```bash
npm i
npm run build
```

### Другие режимы работы с проектом через npm:
```bash
npm run dev
npm run preview
```